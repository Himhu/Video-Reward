<?php
// +----------------------------------------------------------------------
// | CORS跨域配置文件
// +----------------------------------------------------------------------
// | 功能：配置跨域资源共享(CORS)策略
// | 说明：根据环境和安全需求调整CORS设置
// +----------------------------------------------------------------------

return [
    /*
    |--------------------------------------------------------------------------
    | 允许的源域名
    |--------------------------------------------------------------------------
    |
    | 配置允许跨域访问的源域名列表
    | 使用 ['*'] 允许所有域名（仅在开发环境推荐）
    | 生产环境应明确指定允许的域名
    |
    */
    'allowed_origins' => [
        // 开发环境
        'http://localhost:3000',
        'http://localhost:8080',
        'http://127.0.0.1:3000',
        'http://127.0.0.1:8080',
        
        // 生产环境域名（请根据实际情况修改）
        // 'https://your-domain.com',
        // 'https://www.your-domain.com',
        
        // 临时允许所有域名（仅开发环境）
        '*'
    ],

    /*
    |--------------------------------------------------------------------------
    | 允许的HTTP方法
    |--------------------------------------------------------------------------
    |
    | 配置允许的HTTP请求方法
    |
    */
    'allowed_methods' => [
        'GET',
        'POST',
        'PUT',
        'PATCH',
        'DELETE',
        'OPTIONS',
        'HEAD'
    ],

    /*
    |--------------------------------------------------------------------------
    | 允许的请求头
    |--------------------------------------------------------------------------
    |
    | 配置允许的请求头字段
    |
    */
    'allowed_headers' => [
        'Origin',
        'X-Requested-With',
        'Content-Type',
        'Accept',
        'Authorization',
        'X-CSRF-TOKEN',
        'X-File-Name',
        'Cache-Control',
        'Pragma'
    ],

    /*
    |--------------------------------------------------------------------------
    | 是否允许携带凭证
    |--------------------------------------------------------------------------
    |
    | 设置是否允许请求携带凭证（如Cookie、Authorization头等）
    | 注意：当设置为true时，allowed_origins不能为 ['*']
    |
    */
    'allow_credentials' => true,

    /*
    |--------------------------------------------------------------------------
    | 预检请求缓存时间
    |--------------------------------------------------------------------------
    |
    | 设置预检请求的缓存时间（秒）
    | 0 表示不缓存，建议设置为 86400（24小时）
    |
    */
    'max_age' => 86400,

    /*
    |--------------------------------------------------------------------------
    | 暴露的响应头
    |--------------------------------------------------------------------------
    |
    | 配置允许客户端访问的响应头字段
    |
    */
    'expose_headers' => [
        'X-Total-Count',
        'X-Page-Count',
        'X-Per-Page',
        'X-Current-Page'
    ],

    /*
    |--------------------------------------------------------------------------
    | 环境特定配置
    |--------------------------------------------------------------------------
    |
    | 根据不同环境使用不同的CORS配置
    |
    */
    'environments' => [
        'production' => [
            'allowed_origins' => [
                // 生产环境只允许特定域名
                // 'https://your-domain.com',
                // 'https://www.your-domain.com',
            ],
            'allow_credentials' => true,
            'max_age' => 86400
        ],
        
        'development' => [
            'allowed_origins' => ['*'],
            'allow_credentials' => false,
            'max_age' => 0
        ],
        
        'testing' => [
            'allowed_origins' => [
                'http://localhost',
                'http://127.0.0.1'
            ],
            'allow_credentials' => true,
            'max_age' => 0
        ]
    ]
];
